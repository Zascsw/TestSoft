@model List<Dictionary<string, object>>

<h1>Данные из таблицы</h1>

<table>
    <thead>
        <tr>
            @foreach (var columnName in Model.FirstOrDefault()?.Keys ?? Enumerable.Empty<string>())
            {
                <th>@columnName</th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var row in Model)
        {
            <tr>
                @foreach (var columnValue in row.Values)
                {
                    <td>@columnValue</td>
                }
            </tr>
        }
    </tbody>
</table>